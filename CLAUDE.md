# Paperclip (paperclip-repo) — repo entry point

**Load context first, in order:**
1. `../../SKILL.md` — dev-hq: the workspace big picture + house working style.
2. `../SKILL.md` — the `str-desk-context` operating manual (the source of truth).
3. This app's Current State / latest in `../Claude Mem/`.

Don't duplicate the SKILL here — it stays current; this file is a pointer + repo-local mechanics only. Build/ship runbooks live in `../../Flight Plan/Playbooks/`.

## Repo-local mechanics
<!-- add build / dev / test commands and any repo-only conventions here -->
